# Use relative paths so ALL requests go through the Vite proxy on port 5173.
# This avoids CORS issues and works whether accessed via localhost or network IP.
VITE_SIGNAL_SERVER=ws://localhost:5173
VITE_API_BASE=
